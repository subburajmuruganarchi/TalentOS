export enum CandidateStatus {
  ACTIVE = 'active',
  HIRED = 'hired',
  REJECTED = 'rejected',
  WITHDRAWN = 'withdrawn',
}

export enum CandidateSource {
  UPLOAD = 'upload',
  REFERRAL = 'referral',
  PORTAL = 'portal',
  AGENCY = 'agency',
}
