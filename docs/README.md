# TalentOS Documentation

| Document | Description |
|----------|-------------|
| [Architecture](./architecture/README.md) | System design, modules, phases |
| [Environment Setup](../.env.example) | Root environment variables |

## Local Development

1. `npm install` from repo root
2. `npm run docker:up` for MongoDB
3. Copy `.env.example` files per app
4. Run `web`, `api`, and `ai-service` (see root README)
